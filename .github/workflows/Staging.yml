name: Build and Deploy Vue.js App

on:
  push:
    branches:
      - staging

env:
  VITE_API_URL: https://teraerpstaging-fuc8axegceeccqeg.canadacentral-01.azurewebsites.net/api

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: enable corepack
        run: corepack enable
        
      - name: set node
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'yarn'

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Build Vue App
        run: yarn build

      - name: Deploy to Server
        run: |
          sudo rm -rf /var/www/vue-app/staging/dist
          sudo mkdir -p /var/www/vue-app/staging/dist
          sudo cp -r dist/* /var/www/vue-app/staging/dist/

      - name: Reload Nginx
        run: sudo systemctl reload nginx
